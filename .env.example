# Copy to .env.local and fill in your values

# Required — Anthropic API key (server-side only, never exposed to client)
ANTHROPIC_API_KEY=sk-ant-

# Optional — Base URL for API calls from the native client
# Leave empty for same-origin in local dev / Expo Go
# EXPO_PUBLIC_API_BASE_URL=https://your-deployment-url.com
